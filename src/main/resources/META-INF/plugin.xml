<idea-plugin>
    <id>lechuck.intellij-plugin.task</id>
    <name>Taskfile</name>
    <vendor email="lechuckroh@gmail.com" url="https://github.com/lechuckroh/task-intellij-plugin">LechuckRoh</vendor>

    <description><![CDATA[
    Run <code>Taskfile.yml</code> (<a href="https://taskfile.dev">Task</a>) tasks from the IDE.

    <h3>Features</h3>
    <ul>
      <li><b>Run configuration</b>: a <code>Taskfile</code> run configuration type, with
        autocompletion for task names, <code>task</code>-level variables, environment variables,
        and an optional PTY.</li>
      <li><b>Task Explorer tool window</b>: every Taskfile in the project as a tree, with run,
        navigate-to-definition, and toggles for internal tasks and task descriptions. Refreshes
        itself when a Taskfile changes.</li>
      <li><b>Gutter icons</b>: a run icon next to every task in a Taskfile, right in the editor.</li>
      <li><b>Run Anything</b>: type <code>task &lt;name&gt;</code> in the Run Anything popup
        (Ctrl+Ctrl) to complete and run a task.</li>
    </ul>

    <h3>Requirements</h3>
    <ul>
      <li>Install <code>task</code>. See <a href="https://taskfile.dev/#/installation">here</a></li>
      <li>Make sure the <code>task</code> command is in your <code>PATH</code></li>
    </ul>

    <h3>Usage</h3>
    <ul>
      <li>Open 'Run/Debug Configurations' and add a <code>Taskfile</code> configuration, or</li>
      <li>open the <code>Task Explorer</code> tool window and run a task from the tree, or</li>
      <li>click the run icon in the gutter next to a task in a Taskfile, or</li>
      <li>press Ctrl twice and type <code>task &lt;name&gt;</code>.</li>
    </ul>

    ]]></description>

    <change-notes><![CDATA[
    v2.1.0
    <ul>
        <li>Run tasks from the Run Anything popup</li>
    </ul>

    v2.0.0
    <ul>
        <li>Set minimum requirements: 2026.2</li>
        <li>Upgraded IPGP to v2.x</li>
        <li>Added Task Explorer</li>
    </ul>
    ]]></change-notes>

    <!-- please see https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html
         on how to target different products -->
    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.yaml</depends>

    <extensions defaultExtensionNs="com.intellij">
        <configurationType implementation="lechuck.intellij.TaskRunConfigurationType"/>
        <runLineMarkerContributor
                language="yaml"
                implementationClass="lechuck.intellij.TaskLineMarkerProvider"/>
        <toolWindow id="Task Explorer" anchor="left" icon="/icons/taskExplorer.svg"
                    factoryClass="lechuck.intellij.explorer.TaskExplorerToolWindowFactory"/>
        <runAnything.executionProvider
                implementation="lechuck.intellij.runanything.TaskRunAnythingProvider"/>
    </extensions>

    <actions>
        <!-- Add your actions here -->
    </actions>
</idea-plugin>
